package org.example.rawabet.repositories;

import org.example.rawabet.entities.ClubCinema;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClubCinemaRepository extends JpaRepository<ClubCinema, Long> {
}