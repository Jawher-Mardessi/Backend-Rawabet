spring.datasource.url=jdbc:mysql://localhost:3306/rawabet?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# SQL bind values
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Spring Security debug
logging.level.org.springframework.security=DEBUG

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

server.port=8081
server.servlet.context-path=/rawabet

# JWT
jwt.secret=${JWT_SECRET:my-super-secret-key-my-super-secret-key}
# Alternative stronger secret (optional via env only):
# JWT_SECRET=my-super-secret-key-rawabet-2026-changez-moi-en-prod
jwt.expiration=3600000

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=amennahali8@gmail.com
spring.mail.password=jsty nbwo lkjy urti
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Mail debug logs
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.boot.autoconfigure.mail=DEBUG
logging.level.org.springframework.mail.javamail=DEBUG
logging.level.jakarta.mail=DEBUG
logging.level.com.sun.mail=DEBUG

# Frontend URL
app.frontend.url=http://localhost:4200
app.backend.url=http://localhost:8081/rawabet
app.cookie.secure=false

# Super admin email for security alerts
app.super-admin.email=${SUPER_ADMIN_EMAIL:amennahali8@gmail.com}

# Async
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5
spring.task.execution.pool.queue-capacity=25

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.fail-on-self-references=false

# Multipart upload (avatars)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Static upload dir
spring.web.resources.static-locations=file:uploads/,classpath:/static/

ml.api.url=http://localhost:8000
face.api.url=http://localhost:5000
anthropic.api.key=${ANTHROPIC_API_KEY:}
